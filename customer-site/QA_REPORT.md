# QA_REPORT｜確認用サイト

## 実装対象

- URL: `customer-site/index.html`（静的ファイル）
- revision: `akinael/run-e6a353c8-1bbf-4f83-a681-37ce2646c7b2` 作業ツリー

## 実行結果

| 検査 | 結果 |
|---|---|
| `npm run qa` | PASS |
| コンテンツ事実検査 | PASS（確認済みの店名・由布市・営業時間・定休日のみ掲載） |
| 架空連絡先・住所検査 | PASS（電話番号、番地、地図位置を未掲載） |
| HTMLアンカー検査 | PASS |
| JavaScript構文検査 | PASS |

## 実ブラウザ確認

Chrome 152 headless と Firefox headless でローカルファイルのスクリーンショット生成を試行したが、実行環境のブラウザプロセスがそれぞれ Crashpad 初期化エラーとセグメンテーションフォールトで終了し、画面キャプチャを取得できなかった。アプリケーションのコンソールエラーを評価できる状態ではない。

対象とする viewport は 360x800、375x812、390x844、430x932、768x1024、1024x768、1280x800、1440x900。CSS は760pxを境にモバイルナビゲーション・固定CTAを切り替える実装で、上記の静的検査は通過しているが、ブラウザ実機QAはこの実行環境では未完了。

## 公開判定

**FAIL（公開不可）**。実装品質上の自動検査はPASSだが、以下の公開必須情報とブラウザ確認が未完了です。

1. 正式住所、電話番号、地図位置
2. 写真・ロゴの公開利用許諾
3. 指定viewportでの実ブラウザ確認
