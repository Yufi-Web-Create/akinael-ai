# TECH_SPEC

HTML/CSS/JavaScriptのみの静的1ページ構成です。外部フォント、地図、分析、フォーム、外部画像への依存はありません。`app.js` はモバイルメニューと著作権年のみを操作します。

公開候補にする際は、正式電話番号を `tel:` リンクに設定し、確認済みの地図URLを `target="_blank" rel="noopener noreferrer"` で追加します。現版は情報未確定のため、公開ゲート未通過です。
