{
  "name": "akinael-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/platform-server.mjs",
    "worker": "node --env-file-if-exists=.env src/worker.mjs",
    "readiness:worker": "node --env-file-if-exists=.env scripts/worker-readiness.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1116.0"
  }
}
